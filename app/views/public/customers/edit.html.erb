<h1>会員情報編集</h1>

<%= form_with model: @customer, url: customers_path, method: :patch do |f| %>

  <div>
   <%= render 'admin/customers/customer_edit', f: f %>
  </div>

  <%= f.submit '編集内容を保存' %>
<% end %>

<%= link_to "退会する", customers_unsubscribe_path %>


