<div>
  <h1>商品一覧</h1>
  <p>(全<%= @item_count %>件)</p>
</div>

<% @item.each do |item| %>
  <div>
    <%= image_tag item.get_image(500, 400) %>
    <p><%= item.name %></p>
    <p><%= item.add_tax_price %>円</p>
  </div>
<% end %>
