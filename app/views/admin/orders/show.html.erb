<h1><%= @order.customer.last_name %><%= @order.customer.first_name %>さんの注文履歴</h1>

<table>
  <thead>
    <tr>
      <th>購入日時</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @order.each do |order| %>
     <tr>
       <td><%= order.created_at %></td>
       <td><%= order.order_details.amount %></td>
       <td></td>
     </tr>
    <% end %>
  </tbody>
</table>
